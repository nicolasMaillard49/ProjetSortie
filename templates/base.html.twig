<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{% block title %}Document{% endblock %}</title>
    <link rel="stylesheet" href="{{asset('css/bootstrap.min.css') }}">
    {% block stylesheets %}
    {% endblock %}
</head>



<body>


<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand ms-3" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            {% if app.user%}
                <a class="nav-item nav-link" href="{{ path("app_main") }}" >Accueil</a>
                <a class="nav-item nav-link" href="{{ path("app_profil",{id:app.user.id})}}">Mon Profil </a>
                <a class="nav-item nav-link" href="{{ path("app_logout") }}">Se déconnecter</a>
                {% else %}
                    <a class="nav-item nav-link" href="{{ path("app_login") }}" >Se connecter</a>
            {% endif %}
        </div>
    </div>
</nav>
{% block body %}
{% endblock %}

{% block javascriptsBody %}
{% endblock %}
</body>
</html>