{% extends 'base.html.twig' %}

{% block title %}
   Liste des sorties
{% endblock %}

{% block body %}
    <div class="container-fluid">

        <br>
        <div class="row text-center">
            <h1>Liste des sorties</h1>
        </div>
        {#{% for message in app.flashes('succes') %}
            <div class="alert alert-success text-center">
                {{ message }}
            </div>
        {% endfor %}#}
        <br>
        <br>
        <table class="table table-hover table-dark text-center no-padding">
            <thead>
            <tr>
                <th scope="col">nom</th>
                <th scope="col">debut</th>
                <th scope="col">duree</th>
                <th scope="col">date limite</th>
                <th scope="col">Description</th>
            </tr>
            </thead>
            <tbody>
            {% for sortie in sorties %}

                <tr>
                    <td><a class="text-reset" href="{{ path('app_detail_sortie', { id:sortie.id }) }}">{{ sortie.nom }}</a></td>
                    <td>{{sortie.dateHeureDebut|date(" d/m/Y H:i ")}}</td>
                    <td>{{ sortie.duree |date(" H:i ")}}</td>
                    <td>{{sortie.dateLimiteInscription|date(" d/m/Y H:i ")}} </td>
                    <td>{{ sortie.description|slice(0,80) }}...</td>
                </tr>

            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
