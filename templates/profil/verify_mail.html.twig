{% extends 'base.html.twig' %}

{% block title %}Verify email{% endblock %}

{% block body %}

    <div class="container">

    <div class="row ">

    <div class="offset-4 col-4 mt-4 mb-5">

    <div class="row text-center mt-4">
        <h1>Modification du Mot de passe</h1>
    </div>
    {% for message in app.flashes ('Failed') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}
     <form action="{{ path('app_profil_verify_mail') }}" method="post">

    <div class="mt-lg-5">
        <label class="labels">Email :</label>
        <input type="email" class="form-control border border-primary my-2"
               name="email" required>
    </div>
         <button class="btn btn-primary m-lg-4" type="submit">
             valider
         </button>

         <a class="btn btn-primary" href="{{ path('app_login') }}">
             Annuler
         </a>
     </form>

    </div>
    </div>
    </div>



{% endblock %}